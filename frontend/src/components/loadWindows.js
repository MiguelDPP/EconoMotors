import React from 'react';
import styles from '@styles/LoadWindows.module.css';

const loadWindows = () =>{
    return (
        <div className={`${styles.divCenters}`}>
            <div className={`${styles.ldshourglass}`}></div>
        </div>

    );
}
export default loadWindows